# 分潤推廣系統 ‧ 設定說明

這份說明教你如何啟用「推廣連結 + 分潤」功能。整套做法是：

- 每個推廣人有一個**推廣代碼**（如 `alice`），分享網址後面加 `?ref=alice`。
- 客人從他的連結進來下單，訂單會自動標記這位推廣人（採 **last-touch**：以客人最後點到的推廣連結為準）。
- 所有訂單仍進你**同一個** Google Sheet，多一欄「推廣人」。
- 分潤用「未稅金額」計算，你手動改訂單金額後，分潤會**自動重算**。
- 推廣人用一個**查詢小網頁**輸入自己的**查詢金鑰**，就能看到自己的單數、業績、分潤（看不到任何客人個資）。

> 重要觀念：**推廣代碼**是給客人看的網址值（好記，可公開）；**查詢金鑰**是給推廣人查成績的密碼（不好猜，要保密）。兩者分開，代碼被猜到也不會洩漏業績。

---

## 一、檔案異動總覽

我已經幫你改好／新增這些檔案（都在 `website` 資料夾）：

| 檔案 | 異動 |
|------|------|
| `index.html` | 讀取網址 `?ref=`、記住來源、下單時帶上推廣代碼 |
| `GoogleAppsScript.gs` | 訂單多寫「推廣人」欄、白名單驗證、新增查詢 API、新增一鍵建表選單 |
| `seller.html` | **新檔**：推廣人查詢成績的小網頁 |
| `分潤推廣設定說明.md` | **新檔**：本說明 |

你要做的事只有「部署」與「設定推廣人」，下面逐步說明。

---

## 二、設定步驟

### 步驟 1：更新 Google Apps Script

1. 打開你的訂單 Google Sheet → 上方選單 **擴充功能 → Apps Script**。
2. 把 `GoogleAppsScript.gs` 的**全部內容**複製貼上，覆蓋舊的程式碼。
3. 確認最上面的 `SPREADSHEET_ID` 還是你原本填好的試算表 ID（不要被覆蓋成預設文字）。
4. 按 **儲存**（💾）。

### 步驟 2：重新部署 Web App（⚠️ 關鍵，常被忽略）

改了程式碼後，**一定要重新部署，否則網站還是跑舊版**：

1. 右上角 **部署 → 管理部署作業**。
2. 找到現有的部署，按右上角**鉛筆（編輯）**圖示。
3. 「版本」選 **新版本**。
4. 確認「誰可以存取」是 **任何人**（Anyone）。
5. 按 **部署**。
6. 複製出來的 **Web App 網址**。

> 如果網址跟原本一樣，`index.html` 就不用改網址；若 Google 給了新網址，請見步驟 5。

### 步驟 3：一鍵建立分潤工作表

1. 回到 Google Sheet，**重新整理**頁面一次（讓新選單出現）。
2. 上方會多一個選單項目：**📋 帳單工具 → 💰 建立/更新 分潤系統工作表**，點它。
3. 第一次會跳出授權，照著允許即可。
4. 完成後會自動建立兩張分頁：
   - **推廣人名單**：你在這裡新增推廣人。
   - **分潤結算**：自動算好每人的業績與分潤。

### 步驟 4：新增推廣人

到「**推廣人名單**」分頁，一位推廣人加一列：

| 推廣代碼 | 姓名 | 分潤% | 查詢金鑰 | 啟用 | 備註 |
|---------|------|------|---------|------|------|
| alice | 王小美 | 10% | alice-7fK2 | TRUE | |
| bob | 陳大文 | 12% | bob-9pQ4 | TRUE | |

- **推廣代碼**：英數小寫、好記，會出現在網址（如 `alice`）。
- **分潤%**：填 `10%`、`10` 或 `0.1` 都可以（系統會自動換算）。
- **查詢金鑰**：自己設一組不好猜的字串，建議「代碼 + 隨機」如 `alice-7fK2`。這串要私下給推廣人，**不要公開**。
- **啟用**：打 `TRUE` 才生效；想暫停某人就改 `FALSE`。

> 沒登記在名單、或啟用為 FALSE 的代碼，訂單會被視為「直營」（推廣人欄留空），不會誤算分潤。

### 步驟 5：（只有在 GAS 網址改變時才需要）更新前端網址

如果步驟 2 拿到的是**新的** Web App 網址，請把它貼到兩個檔案：

- `index.html` 裡的 `const GAS_WEB_APP_URL = "..."`
- `seller.html` 裡的 `const GAS_WEB_APP_URL = "..."`

兩個要填**同一個**網址。若網址沒變就跳過這步。

### 步驟 6：推上 GitHub

把改好的 `index.html` 和新增的 `seller.html` 上傳到你的 GitHub repo（`ict-pif-order`），跟你平常更新網站一樣的做法即可。等 GitHub Pages 重新發佈後就生效。

---

## 三、怎麼產生「推廣連結」與「查詢連結」

假設推廣人代碼 `alice`、查詢金鑰 `alice-7fK2`，你的網站根網址是
`https://toymock2023.github.io/ict-pif-order/`

**給客人的推廣連結**（Alice 拿去分享）：
```
https://toymock2023.github.io/ict-pif-order/?ref=alice
```

**給 Alice 查成績的連結**（私下給她）：
```
https://toymock2023.github.io/ict-pif-order/seller.html?key=alice-7fK2
```
（她也可以直接打開 `seller.html`，手動輸入金鑰查詢。）

---

## 四、分潤怎麼算、怎麼對帳

- 在「**分潤結算**」分頁，每位推廣人一列，自動顯示：成交訂單數、推廣業績（未稅）、預估分潤。
- 公式直接讀「訂單總覽」的「未稅金額」與「推廣人」兩欄。
- **遇到缺貨要改金額時**：直接到「訂單總覽」修改那筆的「未稅金額」即可，「分潤結算」會自動跟著重算，不必做別的事。
- 推廣人自己用查詢小網頁看到的數字，跟「分潤結算」是同一來源，所以你一改，他那邊重新查詢也會更新。

---

## 五、隱私與防弊重點

- 查詢小網頁**只回傳彙總數字**（單數／業績／分潤），不含任何客人姓名、電話、地址，所以即使金鑰外流也不會洩漏客戶名單。
- 仍建議查詢金鑰用不好猜的字串，並請推廣人自行保管。
- 想停用某位推廣人：把名單裡他的「啟用」改成 `FALSE`，他之後帶來的訂單就不再標記給他（已成立的舊訂單不受影響）。

---

## 六、常見問題

**Q：客人點了 Alice 的連結，但跑去用 Bob 的連結才下單，算誰的？**
A：算 Bob（last-touch，以最後點到的推廣連結為準）。

**Q：客人點過推廣連結，但隔天直接打開網站（沒帶 ?ref=）下單呢？**
A：仍算給他最後點過的那位推廣人——代碼記在他的瀏覽器裡。但若他換裝置或清掉瀏覽器資料，就會變回直營。

**Q：我改了程式碼，但網站沒反應？**
A：九成是忘了「步驟 2 重新部署」。改 GAS 後一定要重新部署成新版本。

**Q：可以一個推廣人不同分潤%嗎？**
A：可以，在名單的「分潤%」欄各自填即可，互不影響。
